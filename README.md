# JS-Calculator
Interactive calculator application built with Vanilla JS. Key implementations include DOM manipulation, string slicing for input control, and CSS Variables for a consistent, themeable interface.
